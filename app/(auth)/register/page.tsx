import { RegisterForm } from '@/components/auth/register-form';

export const metadata = {
  title: 'Reģistrēties | Posterns',
  description: 'Izveidot jaunu Posterns kontu',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
